
const defaultState = {
    application:'Итоговое задание по React, Redux and Router',
    author:'Anthony V. Platonov',
    website:'localhost:8080'
}

const reducer = (state = defaultState, action) => {

    return state

}

export default reducer